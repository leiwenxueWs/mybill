<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mapper.biz.AmountBizMapper">
  <resultMap id="BaseResultMap" type="com.model.auto.Amount">
    <!--
      WARNING - @mbg.generated
      This element is automatically generated by MyBatis Generator, do not modify.
      This element was generated on Mon Apr 22 14:59:42 CST 2019.
    -->
    <id column="id" jdbcType="INTEGER" property="id" />
    <result column="account_number" jdbcType="BIGINT" property="accountNumber" />
    <result column="species_number" jdbcType="BIGINT" property="speciesNumber" />
    <result column="recorder" jdbcType="BIGINT" property="recorder" />
    <result column="amount" jdbcType="BIGINT" property="amount" />
    <result column="record_date" jdbcType="BIGINT" property="recordDate" />
    <result column="create_time" jdbcType="TIMESTAMP" property="createTime" />
    <result column="update_time" jdbcType="TIMESTAMP" property="updateTime" />
    <result column="isdel" jdbcType="BIT" property="isdel" />
  </resultMap>
  <sql id="Base_Column_List">
    <!--
      WARNING - @mbg.generated
      This element is automatically generated by MyBatis Generator, do not modify.
      This element was generated on Mon Apr 22 14:59:42 CST 2019.
    -->
    id, account_number, species_number, recorder, amount, record_date, create_time, update_time, 
    isdel,type
  </sql>

  <select id="selectAmountByAccountNumber" resultMap="BaseResultMap">
    SELECT
    <include refid="Base_Column_List"/>
    FROM bill.amount
    WHERE isdel = 0
    AND account_number = #{accountNumber}
    <if test="type != null">
      AND type = #{type}
    </if>
    ORDER BY record_date
    DESC,
    id DESC
  </select>

  <update id="deleteAmountBySpecies">
    update bill.amount set isdel = 1 where species_number = #{speciesNumber}
  </update>

  <select id="selectSpeciesTotalAmount" resultType="com.dto.common.SpeciesTotalAmountDto">
    select a.speciesTotalAmount,a.species_number as `speciesNumber`,b.species_name as `speciesName`
    from
    (select sum(amount) as `speciesTotalAmount`,species_number
    from bill.amount
    where account_number = #{item.accountNumber}
    and record_date &gt;= #{item.startTime}
    and record_date &lt; #{item.endTime}
    and type = #{item.amountType}
    and isdel = 0
    group by species_number) a
    join
    (select species_number,account_number,species_name
    from bill.species
    where account_number = #{item.accountNumber}
    and species_type = #{item.amountType}
    and isdel = 0) b
    on a.species_number = b.species_number
  </select>

  <select id="selectTotalAmount" resultType="java.lang.Long">
    select sum(amount)
    from bill.amount
    where account_number = #{item.accountNumber}
    and record_date &gt;= #{item.startTime}
    and record_date &lt; #{item.endTime}
    and type = #{item.amountType}
    and isdel = 0
  </select>


</mapper>